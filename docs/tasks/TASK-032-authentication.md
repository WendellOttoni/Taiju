# TASK-032 — Authentication

Implement e-mail/password registration and login with Bun Argon2id password hashing and signed JWT access tokens. Authentication is composed only when the database URL and a 32-character JWT secret are configured.
