# TASK-035 — Reading progress synchronization

Expose authenticated progress upsert and history retrieval endpoints backed by the user-scoped reading-history store, so a client can resume a chapter across sessions and devices.
