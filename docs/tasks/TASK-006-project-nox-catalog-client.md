# TASK-006 — Project Nox catalog client

Fetch the Project Nox `index.pb` bytes with explicit timeout/cancellation and deterministic fixture injection. Decoding, source enumeration and extension execution are intentionally deferred.
