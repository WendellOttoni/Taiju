# TASK-013 — Search UI

Create the first web search screen. It must call only Taiju's search endpoint, debounce requests, present loading/error/empty states, and render normalized result cards responsively.
