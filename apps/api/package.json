{
  "name": "@taiju/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "^4.10.4",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
